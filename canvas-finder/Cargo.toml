[package]
name = "canvas-finder"
version = "0.1.0"
edition = "2021"

[dependencies]
wolframe-spotify-canvas = "1.0.4"
tokio = { version = "1", features = ["rt", "macros"] }

[profile.release]
opt-level = "s"
lto = true
strip = true
codegen-units = 1
